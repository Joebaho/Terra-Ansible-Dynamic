- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: true

- name: Enable nginx
  service:
    name: nginx
    state: started
    enabled: true

- name: Deploy index
  template:
    src: "{{ env }}.html"
    dest: /var/www/html/index.html
  notify: restart nginx